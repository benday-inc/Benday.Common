name: Benday.Common.Interfaces CI

on:
  push:
    branches: [ "main" ]
    paths:
      - 'Benday.Common.Interfaces/**'
      - 'Benday.Common.Interfaces.UnitTests/**'
      - '.github/workflows/benday-common-interfaces.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'Benday.Common.Interfaces/**'
      - 'Benday.Common.Interfaces.UnitTests/**'
      - '.github/workflows/benday-common-interfaces.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore Benday.Common.Interfaces
        run: dotnet restore Benday.Common.Interfaces/Benday.Common.Interfaces.csproj

      - name: Build Benday.Common.Interfaces (Release)
        run: dotnet build Benday.Common.Interfaces/Benday.Common.Interfaces.csproj --configuration Release --no-restore

      - name: Restore UnitTests
        run: dotnet restore Benday.Common.Interfaces.UnitTests/Benday.Common.Interfaces.UnitTests.csproj

      - name: Build UnitTests (Release)
        run: dotnet build Benday.Common.Interfaces.UnitTests/Benday.Common.Interfaces.UnitTests.csproj --configuration Release --no-restore

      - name: Test (Release)
        run: dotnet test Benday.Common.Interfaces.UnitTests/Benday.Common.Interfaces.UnitTests.csproj --configuration Release --no-build --verbosity normal

      - name: Pack Benday.Common.Interfaces
        run: dotnet pack Benday.Common.Interfaces/Benday.Common.Interfaces.csproj --no-build --configuration Release --output ./artifacts

      - name: Upload NuGet packages
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages-common-interfaces
          path: ./artifacts/*.nupkg

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: nuget-deploy
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: nuget-packages-common-interfaces

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Push to NuGet
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: |
          if [ -z "$NUGET_API_KEY" ]; then
            echo "Error: NUGET_API_KEY secret is not set"
            exit 1
          fi
          dotnet nuget push *.nupkg -k "$NUGET_API_KEY" -s https://api.nuget.org/v3/index.json --skip-duplicate
